#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y;
    printf("Inserire due valori interi di cui fare la media\n");
    scanf("%d %d", &x, &y);
    printf("La media \212 %d", ((x + y) / 2));
    return 0;
}
